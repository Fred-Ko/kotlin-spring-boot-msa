package com.ddd.user.application.command.service

import com.ddd.user.application.dto.command.RegisterUserDto
import org.springframework.stereotype.Service

@Service
class RegisterUserApplicationService {

    fun registerUser(dto: RegisterUserDto) {
        // 여기에 사용자 등록 로직 구현
        TODO("Implement user registration logic")
    }
}
